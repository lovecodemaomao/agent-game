# Tasks: sim-platform-and-agents

## 1. 项目骨架与配置（P0）

- [x] 1.1 创建项目结构（`sim/{engine,judge,llmproxy,sandbox,news,web}`、`agents/{framework,competitor,baseline}`、`config/`、`tests/`）与 `run.sh` 入口，确认目录与空 `__init__.py` 就位
- [x] 1.2 编写 `config/engine.yaml`（地图/昼夜/波次/寻路/新闻影响/宝藏模板/任务超时等全部未定义机制参数）与加载器，验证修改波次系数后加载值随之变化
- [x] 1.3 编写 `config/doctrines/` 教义预设卡（≥3 张：均衡/龟缩/经济）与 `config/baselines/` 对手策略卡（龟缩防御卡），验证 JSON schema 校验通过
- [x] 1.4 将 `deepseek-key.md` 加入 `.gitignore` 并创建 `deepseek-key.example.md` 模板，验证 git status 不再跟踪真实密钥文件

## 2. Agent 公共骨架（P0）

- [x] 2.1 实现 HTTP server 骨架（`bash run.sh port` 启动、POST 回合接口、返回 Response 2.1 三字段结构），用 curl 发送 demo request.txt 能收到合法骨架 response
- [x] 2.2 实现出站校验器（schema 层：动作码全集/必填字段/类型/坐标域；语义层：ID 归属/昼夜门/角色类型门/多目标数=武器等级/加特林锥形≤90°），单测覆盖全部 12 个动作码的合法与非法样例（非法样例含白天 attack、move 缺 targetPos、use DizzyWeapon 缺 targetPos 等 errorCode=4 场景）
- [x] 2.3 实现回合编排（时间预算、异常兜底空响应、进程存活），用故意抛异常的策略层验证仍按时返回合法报文
- [x] 2.4 实现 LLM 异步通道管理（prompt 发送/llmResp 读取/未完成调用表/每日 3 次配额自律/任务期豁免状态），单测验证第 4 次 prompt 被本地拦截、任务期不计数

## 3. 判题循环（P0）

- [x] 3.1 实现进程编排（拉起双方 agent 子进程、端口分配、优雅退出），运行后两个 agent 端口均可访问
- [x] 3.2 实现 HTTP 回合循环（连接 10s/响应 5s 死线、双方并行请求、回合推进），用两个骨架 agent 跑通 1300 回合空转
- [x] 3.3 实现异常计数（三类异常计次、指令执行失败不计次、5 次停调度），单测覆盖超时/格式错误/指令错误/碰撞失败四种情形的计数行为
- [x] 3.4 实现对局记录输出（JSONL：双方 request/response 原文+引擎事件+积分快照），一场结束后文件可被 json 逐行解析且回合数完整

## 4. 战斗引擎（P1）

- [ ] 4.1 实现世界状态与初始化（地图/双方单位/ID 分配表/矿区随机生成不落可建造区），断言初始状态与任务书 4.5.3 一致（金币 75、开拓者×1 工人×2）
- [ ] 4.2 实现可见性纯函数 `compute_request_view`，以 docs/request.txt 为 golden 快照做字段级 diff 验收
- [ ] 4.3 实现移动与碰撞结算（8 向/切比雪夫距离/三情形碰撞/对角穿行），golden 测试覆盖每类碰撞
- [ ] 4.4 实现经济系统（采集/矿枯竭刷新/不足平分/贩卖/购买/背包容量），单测覆盖矿采 10 次消失、平分兜底、背包不足购买失败
- [ ] 4.5 实现建造/拆除/升级（白天门/可建造区域/武器上限 3/围墙耗石/升级回满血/满级不消耗券），单测覆盖每条规则含"拆墙不返还"
- [ ] 4.6 实现武器攻击结算（加特林锥形+弹道最近命中/电磁炮穿透扣能/火箭溅射叠加+冷却/结算顺序：物品>攻击>机器人移动/伤害回合末统一结算），golden 测试覆盖 design 引用的电磁炮穿透样例（能量 20 对 5+60 血）
- [ ] 4.7 实现机器人系统（夜间出现/攻击阻挡者/清晨清除/dizzy 5 回合/召唤令叠加+targetTeam/参数化寻路与波次），单测验证切换寻路模型后行为变化、召唤令次日生效
- [ ] 4.8 实现积分与胜负判定（score1 含速度加成与通过率/score2 击杀/score3 生存/先摧毁判负/1300 回合结束），构造对局验证各判定分支
- [x] 4.9 引擎集成测试：驱动两个基线策略卡跑完整 10 天对局，全程无引擎异常、积分单调合理、对局记录完整

## 5. 对手基线 Agent（P1）

- [x] 5.1 基于骨架实现固定策略卡执行（白天采集建墙买券、夜晚全员操炮、非任务期 prompt 恒空），验证整场对局的非任务期 prompt 全为空字符串
- [x] 5.2 验证行为确定性：相同种子重放两场对局，对手指令序列逐回合一致

## 6. 前端可视化（P1）

- [x] 6.1 实现对局记录 API 与静态页服务（对局列表/单场 JSONL 加载），浏览器可打开并加载一场记录
- [x] 6.2 实现战场 Canvas 渲染（41×32 网格/全部单位按 roleType 着色/回合步进），步进时单位状态与记录中该回合一致
- [x] 6.3 实现回合动作展示（双方动作列表/目标标记/有效无效标色/视野切换），切到我方视角时视野外敌单位隐藏
- [x] 6.4 实现资源面板与积分曲线 + 过滤式事件日志（按队伍/类型/回合过滤、点击跳转），数值与对局记录抽验一致

## 7. LLM 代理（P2）

- [x] 7.1 实现 deepseek 客户端（密钥读取/重试退避/超时/磁盘缓存），真实调用一次验证连通与缓存命中（第二次同 prompt 不再计费）
- [x] 7.2 实现三上下文隔离（我方/新闻/对手独立会话历史），测试验证我方上下文中检索不到对手与新闻历史
- [x] 7.3 实现配额与豁免（每日 3 次/日界重置/超限 errorCode=5/任务期不计次），集成测试覆盖超限报错与任务期豁免两个场景

## 8. 新闻系统（P2）

- [x] 8.1 实现事件参数采样器（矿价事件参数/宝藏谜题答案三元组确定性锁定），单测验证相同种子产出相同答案
- [x] 8.2 实现 LLM 措辞生成（参数→叙事文本，独立上下文），人工抽检 5 天新闻：文本可推断参数且不泄漏数值
- [x] 8.3 实现新闻影响结算（矿价倍率/停采/恢复时间表/召唤宝藏结果码 1-4），单测覆盖塌方三段式影响与祭品错误消耗（返回码 3 且扣祭品）

## 9. 沙盒（P2）

- [x] 9.1 实现每队沙盒子进程（shell+python 执行/15 秒超时/`[exitCode:N]`/`[TIMEOUT]`/64KB 截断格式回填），单测覆盖 echo/超时/大输出三类回填格式
- [x] 9.2 实现 mock 任务 API（天气查询等）与禁网标记，验证沙盒内可调 mock API、外网域名不可达
- [x] 9.3 实现任务期门控（仅任务期允许 executeCmd），非任务期提交时字段被忽略

## 10. 任务求解代理（P2）

- [x] 10.1 实现 phaseTask 解析与任务态管理（领取/执行/提交/超时状态机 + 30 回合冷却跟踪），状态流转单测
- [x] 10.2 实现 LLM+沙盒流水线（prompt 思考→executeCmd 探索→submitAnswer），在 mock 天气任务上完成首次探索并提交正确答案
- [x] 10.3 实现 SOP 沉淀与复用（首次任务脚本落盘、同类任务直接执行），验证第二个同类任务的完成回合数显著少于首个
- [x] 10.4 沙盒持久性实测：任务 A 写标记文件、任务 B 读取，确认跨任务持久性并回填 design 开放问题 3

## 11. 我方 Agent 内核（P3）

- [x] 11.1 实现 OpponentModel（三通道逐回合 diff：建筑/等级/血量变化、击杀 DPS 指纹、骚扰检测、任务活动推断；滚动时序特征），用对局记录离线回放验证建墙/击杀/骚扰三类事件捕获正确
- [x] 11.2 实现教义×旋钮引擎（参数向量加载/硬夹紧/日界原子热替换/日常调度零 LLM：采集-贩卖-建造-火控分配-升级决策），验证普通回合 prompt 为空且全部指令经校验器出站
- [x] 11.3 实现每日 LLM 分析（日界合并 prompt：仪表盘+教义索引+新闻；llmResp JSON 校验+夹紧+次日生效；兜底链 LLM→昨日教义→全局默认），单测覆盖合法输出换参、非法输出兜底两个场景
- [x] 11.4 实现新闻推理应用（囤卖套利时机/采集偏好调整）与宝藏求解（传闻收敛→三要素高信心→召唤执行），单测覆盖塌方囤卖与祭品错误不重复召唤

## 12. 端到端验收（P3）

- [x] 12.1 完整对战验收：一条命令跑通"启动→10 天对局→对局记录→前端回放"，全链路无人工干预
- [x] 12.2 合规审计：对整场双方 response 做协议检查，零"指令错误"级非法报文；异常计数 < 5
- [x] 12.3 策略对比复盘：我方（默认均衡教义）vs 对手（龟缩卡）各跑正反两场，前端查看积分曲线与关键回合（日界换参/任务完成/宝藏召唤）成立
- [x] 12.4 回填 design 开放问题清单中已实测项（1-6），未决项标注后续计划
